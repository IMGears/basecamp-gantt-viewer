<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><%= title %></h1>
    </header>

    <main>
      <% if (user) { %>
        <p>Welcome, <%= user.displayName %>!</p>
        <a href="/auth/logout" class="btn">Logout</a>
        <a href="/gantt" class="btn btn-primary">View Gantt Chart</a>
      <% } else { %>
        <p>View your Basecamp tasks as a Gantt Chart</p>
        <a href="/auth/google" class="btn btn-primary">Sign in with Google</a>
      <% } %>
    </main>
  </div>
</body>
</html>
